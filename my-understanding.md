# My Understanding

ตอบแต่ละคำถามด้วยคำพูดของคุณเอง ไม่มีคำถามหลอกล่อ

เป้าหมายไม่ใช่คำตอบที่สมบูรณ์แบบ แต่เป็นคำตอบที่ตรงไปตรงมา เขียนเหมือนกำลังอธิบายให้เพื่อนที่ไม่เคยใช้ Express หรือ React ฟัง Assessment นี้ไม่มีวิดีโอ ดังนั้นเอกสารนี้คือจุดที่ความเข้าใจของคุณจะถูกประเมินจริง ๆ — ให้ทำอย่างตั้งใจ

อย่า copy จาก documentation, comment ในโค้ดของคุณ, หรือ AI output ถ้าไม่แน่ใจเรื่องไหน ให้เขียนเท่าที่คุณเข้าใจ และระบุว่าส่วนไหนยังไม่แน่ใจ

---

## AI Code Contribution

rate ตัวเองอย่างตรงไปตรงมาโดยใช้ scale ด้านล่างนี้ rating นี้ไม่ถูกให้คะแนนด้วยตัวมันเอง — ไม่มีตัวเลขไหนที่ "ดีที่สุด" สิ่งที่สำคัญคือมันตรงไปตรงมาและตรงกับสิ่งที่โค้ดและคำตอบของคุณแสดงให้เห็นจริง

| Rating | คำอธิบาย |
|---|---|
| 0 | **ไม่ได้ใช้ AI เลย** ฉันไม่ได้ใช้ AI สร้างโค้ด อธิบาย concept debug หรือสอนฉันเลย |
| 1 | **ใช้ AI เพื่อเรียนรู้เท่านั้น** ฉันไม่ได้ใช้ AI สร้างโค้ด แต่ใช้ AI ช่วยอธิบาย concept ไขข้อสงสัยเรื่อง error หรือช่วยให้เข้าใจมากขึ้น |
| 2 | **เขียนโค้ดเองผสมกับใช้ AI ช่วย** ฉันเขียนโค้ดเองบางส่วน และใช้โค้ดที่ AI สร้างบางส่วน รวมถึงใช้ AI ช่วยให้เข้าใจ debug หรือปรับปรุง solution ของฉัน |
| 3 | **เรียนรู้จากโค้ดที่ AI สร้าง แล้วเขียนเอง** AI สร้างโค้ดตัวอย่างหรือให้คำแนะนำ แต่ฉันใช้ความเข้าใจนั้นมาเขียนหรือปรับโค้ดสุดท้ายด้วยตัวเอง |
| 4 | **AI สร้างโค้ดให้ แต่ฉันเข้าใจมันอย่างครบถ้วน** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด แต่ฉันอธิบายได้ว่ามันทำงานอย่างไร ทำไมถึงทำงาน และส่วนหลัก ๆ เชื่อมกันอย่างไร |
| 5 | **AI สร้างโค้ดให้ แต่เข้าใจอย่างจำกัด** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด และฉันไม่สามารถอธิบายได้อย่างมั่นใจว่าทุกอย่างทำงานอย่างไรหรือทำไมถึงทำงาน |

**Rating ของฉัน:** 2

> ถ้า rate ไว้ **2 ขึ้นไป** ให้ตอบส่วน "AI Process" ที่ท้ายเอกสารนี้ด้วย

---

## Backend

**1. HTTP method แต่ละตัวในแอปของคุณหมายถึงอะไร — GET, POST, PUT or PATCH, และ DELETE? ทำไมเราถึงใช้ method ต่างกัน แทนที่จะใช้ POST สำหรับทุกอย่าง?**

*คำตอบของคุณ:*
- **GET คือ การเรียกข้อมูลใดๆ ในระบบ
- **POST คือ การสร้างหรือส่งข้อมูลใหม่ ขึ้นไปในระบบ
- **PUT/PATCH คือ การแก้ไขข้อมูลใดๆ ในระบบ
- **PATCH หมายถึงการ "แก้ไขข้อมูลบางส่วน" ของสินค้าที่มีอยู่เดิมตาม id ที่ระบุ เช่น แก้เฉพาะราคาหรือจำนวนสต็อก (ต่างจาก PUT ที่มักเป็นการแทนที่ข้อมูลทั้งชิ้น)
- **DELETE คือ การลบข้อมูลใดๆ ออกจากระบบตาม id ที่ระบุ

---

**2. `express.json()` คืออะไร และจะเกิดอะไรขึ้นถ้าคุณไม่ใส่มัน?**

*คำตอบของคุณ:*
คือ middleware ที่มากับ express ที่แปลง req body จาก json เป็น js object ถ้าไม่ใส่ express จะไม่รู้ว่าจะแปลงยังไง ทำให้ req.body มีค่าเป็น undefined แล้วจะใช้ HTTP Method ไม่ได้

---

**3. `req.body`, `req.params`, และ `req.query` ต่างกันอย่างไร? ยกตัวอย่างจริงจาก API ของคุณสำหรับแต่ละตัว**

*คำตอบของคุณ:*

- req.body คือ object ก้อนใหญ่สุด ที่ส่งมาจาก client ยกตัวอย่างคือ ยิง POST /products จะได้ {name: "Ferrari cap", ...} เป็นต้นครับ
- req.params คือตัวแปรใน Url Path ยกตัวอย่าง ตอนยิง PATCH /product/1 จะได้ req.params.id เป็น "1"
- req.query คือค่าที่ส่งต่อท้าย URL หลังเครื่องหมาย ? ใช้เพื่อค้นหา หรือกรอก เช่น ยิง GET /product?name=Cap จะได้ req.query.name เป็น Cap

---

**4. HTTP status codes คืออะไร? ระบุรายการ status code ทุกตัวที่คุณใช้ใน API และอธิบายว่าทำไมถึงเลือกใช้ในแต่ละสถานการณ์**

*คำตอบของคุณ:*
HTTP Status Codes คือ รหัสที่บอก Response เพื่อสื่อสารบอก Client สั้นๆ ว่าผลลัพธ์จะเป็นยังไง เช่น 200 คือ OK, 404 คือ Not Found, 500 คือ Internal Server Error

---

**5. middleware คืออะไร? อธิบายด้วยคำพูดของคุณเองว่ามันทำอะไร พร้อมยกตัวอย่าง 1 อย่างจากโค้ดของคุณ**

*คำตอบของคุณ:*
Middleware คือ "ตัวกลาง" ที่ อยู่ระหว่าง client และ server และเป็นตัวสกัดกั้น Request แล้วส่งไปให้ Router เช่น 

---

**6. ทำไม order ของ middleware ใน Express ถึงสำคัญ? จะเกิดอะไรขึ้นถ้า order ผิด?**

*คำตอบของคุณ:*
ลำดับ (Order) ของ Middleware สำคัญมาก เพราะ Express จะประมวลผลคำสั่งเรียงตามลำดับจากบนลงล่าง (Top-to-Bottom) ตามที่เราเขียนไว้ในไฟล์โค้ด

**ถ้าเรียงลำดับผิดจะเกิดปัญหา เช่น:**
1. **ถ้าเอา `express.json()` ไปวางไว้หลัง Router:** เมื่อมีคำขอแบบ POST ส่งเข้ามา ตัว Router จะทำงานก่อนที่ JSON จะถูกแปลง ทำให้ใน route มองเห็น `req.body` เป็น `undefined` และทำงานต่อไม่ได้
2. **ถ้าเอา Error Handling Middleware ไปวางไว้บนสุด:** ตัวดักจับ Error (ที่มีพารามิเตอร์ 4 ตัว `err, req, res, next`) จะต้องอยู่ **ล่างสุดของ Middleware chain เสมอ** เพื่อรอรับ Error ที่ถูกส่งต่อ (ผ่าน `next(err)`) มาจาก route ต่าง ๆ ด้านบน ถ้าไปวางไว้บนสุด มันจะไม่สามารถดักจับ error ที่เกิดขึ้นใน route handler ได้เลย

---

**7. อธิบายทีละขั้นตอนว่าเกิดอะไรขึ้นบน server เมื่อมี POST request ถูกส่งไปที่ `/products`**

*คำตอบของคุณ:*
request ส่งไปที่ port 3001 -> วิ่งผ่าน Cors เพื่อให้อนุญาตข้าม Origin -> express.json() แปลง Json เป็น object ใน req.body -> วิ่งผ่าน Logger Middleware -> วิ่งเข้า Router เจอ router.post("/product", ...) -> validation ว่า name, price, quantity มาครบไหม -> ถ้าครบ สร้าง newProduct -> push newProduct เข้า products array -> respond ไป client พร้อม status และ ข้อมูล product ใหม่

---

**8. CRUD คืออะไร? จับคู่แต่ละ operation กับ HTTP method และ route ที่คุณใช้ใน API**

*คำตอบของคุณ:*
CRUD คือคำย่อของ 4 การกระทำพื้นฐานในการจัดการฐานข้อมูลหรือ Resource ในระบบ:

1. **C - Create: POST/products (สร้างข้อมูลใหม่):**
2. **R - Read GET /products (อ่าน/ดึงข้อมูล):**
3. **U - Update: PATCH /product/:id (แก้ไขข้อมูล):**
4. **D - Delete GET /products/:id (ลบข้อมูลชิ้นเดียว):**

---

**9. API ของคุณตอบสนองอย่างไรเมื่อมีอะไรผิดพลาด — เช่น เมื่อ product ตาม ID ที่ระบุไม่มีอยู่จริง?**

*คำตอบของคุณ:*
- **กรณีหา ID ไม่พบ (เช่น `GET`, `PATCH`, หรือ `DELETE` ตาม ID ที่ไม่มี):**
  ฟังก์ชันจะใช้ `.find()` ค้นหาใน array ถ้าไม่พบตัวแปรจะเป็น `undefined` โค้ดจะดักด้วย `if (!product)` แล้วส่ง HTTP Status `404 Not Found` กลับไปทันที พร้อมข้อความ JSON:
  ```json
  { "error": "Product not found!" }
  ```
- **กรณีส่งข้อมูลไม่ครบถ้วน (Validation Fail):**
  ใน POST หรือ PATCH ถ้าขาด field ที่จำเป็น จะตอบกลับด้วย HTTP Status `400 Bad Request` พร้อมข้อความเตือน เช่น `{ "error": "name, price, quantity are required!" }`
- **กรณีเกิดข้อผิดพลาดไม่คาดคิดบนเซิร์ฟเวอร์ (Runtime Error):**
  โค้ดทุก route ครอบด้วยบล็อก `try...catch` เมื่อเกิด error จะส่งต่อไปที่ `next(err)` และ Centralized Error Handling Middleware จะตอบกลับด้วย HTTP Status `500 Internal Server Error` พร้อมรายละเอียด error สั้น ๆ เพื่อไม่ให้ระบบล่ม

---

## Frontend & Integration

**10. CORS คืออะไร และแก้ปัญหาอะไร? ถ้าไม่ได้ config ไว้บน server ของคุณ คุณจะเห็นอะไรใน browser?**

*คำตอบของคุณ:*
CORS (Cross-Origin Resource Sharing) เป็นกลไกความปลอดภัยของเว็บเบราว์เซอร์ ที่ป้องกันไม่ให้สคริปต์บนหน้าเว็บจาก Origin หนึ่ง (เช่น Frontend พอร์ต `5173`) แอบไปดึงข้อมูลจากอีก Origin หนึ่ง (เช่น Backend พอร์ต `3001`) โดยไม่ได้รับอนุญาต

**แก้ปัญหาอะไร:** แก้ปัญหาให้ระบบที่แยกบ้านกันอยู่ระหว่าง Frontend และ Backend สามารถแลกเปลี่ยนข้อมูลกันได้อย่างถูกต้องและปลอดภัย

**ถ้าไม่ได้ config ไว้:** เบราว์เซอร์จะบล็อกคำขอทันที หน้าเว็บจะไม่ได้รับข้อมูล 

---

**11. แอป React ของคุณ fetch ข้อมูลจาก API ที่ไหน? อธิบายว่า `useEffect` ในโค้ดนั้นทำอะไร และทำไมถึงเรียก fetch ตรง ๆ ใน component body ไม่ได้**

*คำตอบของคุณ:*
- ในแอปของเรา fetch ข้อมูลจาก API ผ่านฟังก์ชัน `fetchProducts()` ภายใน Component `Home`
- **`useEffect` ทำหน้าที่อะไร:** ทำหน้าที่จัดการ Side Effect (การติดต่อกับโลกภายนอก) โดยสั่งให้ฟังก์ชัน `fetchProducts()` ทำงานตอนที่ Component ถูกเรนเดอร์ครั้งแรก (Mount) และจะทำงานซ้ำอีกครั้งเมื่อค่าใน dependency เช่น `searchTerm` มีการเปลี่ยนแปลง (ใช้ร่วมกับ Debounce ในการค้นหา)
- **ทำไมเรียก fetch ตรง ๆ ใน component body ไม่ได้:** 
  เพราะ Component Body จะถูกรันใหม่ทุกครั้งที่มีการ Re-render ถ้าเราใส่ fetch ตรง ๆ ใน body:
  1. หน้าเว็บโหลด -> body ทำงาน -> ยิง fetch
  2. เมื่อ fetch สำเร็จ ข้อมูลถูกเซ็ตผ่าน `setProducts(data)`
  3. เมื่อ State เปลี่ยน -> React สั่ง Re-render Component ใหม่
  4. เมื่อ Re-render -> body ถูกรันซ้ำ -> ยิง fetch ใหม่อีกรอบ
  เหตุการณ์นี้จะวนลูปซ้ำไปเรื่อย ๆ กลายเป็น **Infinite Loop** ยิง API ถี่ไม่หยุดจนทำให้เบราว์เซอร์ค้างหรือเซิร์ฟเวอร์ล่มทันที

---

**12. API base URL ของคุณถูกกำหนดไว้ที่ไหน และทำไมถึงเลือกเก็บไว้ตรงนั้น แทนที่จะ hardcode ไว้ในทุก fetch call?**

*คำตอบของคุณ:*
กำหนดไว้ในไฟล์ `.env` ของฝั่ง `client/` ผ่านตัวแปร `VITE_API_URL` และนำมาประกาศเป็นตัวแปรส่วนกลางด้านบนสุดของไฟล์ `Home.jsx`:
```javascript
const API_URL = import.meta.env.VITE_API_URL || "http://localhost:3001/products";
```

**ทำไมถึงไม่ Hardcode ไว้ในทุก fetch call:**
1. **ง่ายต่อการบำรุงรักษา (Maintainability):** ถ้าวันหน้ามีการเปลี่ยน Port ของเซิร์ฟเวอร์ หรือนำระบบไป deploy ขึ้น Production โดเมนจริง เราแก้ที่ไฟล์ config จุดเดียว โค้ดทั้งระบบจะอัปเดตตามทันที
2. **ลดความผิดพลาด (Clean & Error-free):** การพิมพ์ซ้ำ ๆ หลายจุดเสี่ยงต่อการพิมพ์ตกหล่น เช่น ลืมตัวอักษรบางตัว หรือพิมพ์ path ผิด
3. **แยก Environment ชัดเจน:** ช่วยให้เราสลับการทำงานระหว่างเครื่อง Local ตอนพัฒนา กับระบบ Production ตอนใช้งานจริงได้อย่างง่ายดาย

---

**13. เลือก action หนึ่งในแอปของคุณ — เช่น การลบ product อธิบายการเดินทางแบบครบวงจร (full round trip): เกิดอะไรขึ้นตั้งแต่ผู้ใช้คลิกปุ่ม ไปจนถึง request ไปถึง server จนถึงหน้าจออัปเดตด้วย list ใหม่**

*คำตอบของคุณ (Delete Product):*
1. **ผู้ใช้คลิกปุ่ม:** ผู้ใช้คลิกปุ่ม `[DELETE]` ที่แถวของอัลบั้มที่ต้องการลบในตาราง (เช่น อัลบั้ม ID: `"1"`)
2. **เรียกฟังก์ชัน & ยืนยัน:** เกิด Event คลิก เรียกฟังก์ชัน `deleteProduct(item.id)` มีหน้าต่าง `window.confirm` เด้งขึ้นมาถามความแน่ใจ เมื่อกดยืนยัน โค้ดจะเริ่มส่งคำขอ
3. **ส่งคำขอ (HTTP Request):** ฝั่ง React ทำการเรียกคำสั่ง `fetch("http://localhost:3001/products/1", { method: "DELETE" })` ส่งคำขอออกไปที่ Backend
4. **เซิร์ฟเวอร์ประมวลผล:** 
   - Express รับคำขอผ่าน middleware ต่าง ๆ มาจนถึง `router.delete("/products/:id")`
   - เซิร์ฟเวอร์อ่านค่า ID `"1"` จาก `req.params.id`
   - เซิร์ฟเวอร์ค้นหาตำแหน่งของสินค้านั้นใน in-memory array ด้วย `.find()` และ `.indexOf()`
   - ใช้คำสั่ง `products.splice(index, 1)` ลบสินค้านั้นออกจาก Array
   - เซิร์ฟเวอร์ส่ง HTTP Status `204 No Content` หรือ Status 200 ยืนยันกลับมา
5. **รับ Response:** ฝั่ง React ได้รับผลตอบรับและตรวจสอบ `response.ok` เมื่อพบว่าสำเร็จ จะขึ้น alert แจ้งเตือนผู้ใช้
6. **อัปเดตหน้าจอ (Re-render):** React สั่งเรียก `fetchProducts()` เพื่อดึงลิสต์สินค้าล่าสุดจาก Server -> เมื่อได้ข้อมูลใหม่ จะเรียก `setProducts(updatedList)` -> ค่าใน State เปลี่ยนแปลง React จึง Re-render ตารางใหม่ ทำให้แถวสินค้าที่เพิ่งลบหายไปจากหน้าจอทันทีโดยที่ผู้ใช้ไม่ต้องรีเฟรชเบราว์เซอร์เอง

---

**14. แอปของคุณแสดงอะไรให้ผู้ใช้เห็นระหว่างที่ข้อมูลกำลังโหลด และแสดงอะไรถ้า fetch ล้มเหลว (เช่น server ไม่ได้รันอยู่)? ทำไมเรื่องนี้ถึงสำคัญ?**

*คำตอบของคุณ:*
- **ระหว่างกำลังโหลด (Loading State):** แอปจะแสดงกล่องข้อความพร้อมไอคอนหมุนนีออนสไตล์ NCT 127 ว่า `"FILTERING NEO DATABASE..."` หรือ `"กำลังโหลดข้อมูลสินค้า..."`
- **ถ้า fetch ล้มเหลว (Error State):** แอปจะแสดงกล่องข้อความสีแดงเตือนชัดเจน เช่น `"⚠️ CONNECTION ERROR: Cannot connect to server"` พร้อมปุ่มให้กด `"RETRY CONNECTION"` ได้ทันที
- **ทำไมเรื่องนี้ถึงสำคัญ (User Experience):** 
  สำคัญต่อประสบการณ์ของผู้ใช้งานมาก เพราะหากไม่มี Loading ผู้ใช้จะเห็นเพียงหน้าจอว่างเปล่าและอาจเข้าใจว่าระบบค้าง และหากเซิร์ฟเวอร์ดับแล้วไม่มี Error State แจ้งเตือน ผู้ใช้จะไม่รู้เลยว่าเกิดปัญหาอะไรขึ้นและอาจกดปุ่มซ้ำ ๆ โดยไม่รู้สาเหตุ (Silent Failure) การแจ้งเตือนที่ชัดเจนช่วยให้ผู้ใช้งานเข้าใจสถานะของระบบและรู้ว่าควรทำอย่างไรต่อ

---

**15. หลังจากที่คุณ add, edit, หรือ delete product แล้ว list บนหน้าจอของคุณอัปเดตโดยไม่ต้อง refresh หน้าเว็บ อธิบายว่าทำไมถึงเป็นแบบนั้น — อะไรที่ทำให้ React re-render ด้วยข้อมูลใหม่?**

*คำตอบของคุณ:*
เป็นเพราะกลไกการทำงานแบบ **State-driven UI** ของ React

ในโค้ดของเรา ข้อมูลสินค้าที่แสดงบนตารางถูกผูกอยู่กับ State ที่ชื่อว่า `products` (สร้างจาก `useState`)
เมื่อเราทำคำสั่ง Add, Edit, หรือ Delete สำเร็จ เราจะสั่งให้ดึงข้อมูลชุดล่าสุดจาก Backend และอัปเดต State ผ่านฟังก์ชัน Setter:
- setProducts(data)

เมื่อฟังก์ชัน `setProducts` ถูกเรียก React จะเปรียบเทียบข้อมูลใหม่กับข้อมูลเดิม และตรวจพบว่า State มีการเปลี่ยนแปลง (State Update) สิ่งนี้จะสั่งให้ React ทำการ **Schedule a Re-render** โดยอัตโนมัติ React จะคำนวณส่วนต่างผ่าน Virtual DOM แล้วอัปเดตเฉพาะ DOM ในส่วนของตารางบนหน้าจอจริงให้เป็นข้อมูลใหม่ทันที โดยไม่จำเป็นต้องโหลดหน้าเว็บใหม่ทั้งหน้า (No Page Reload)

---

**16. ส่วนไหนที่ยากที่สุดในการเชื่อมแอป React ของคุณเข้ากับ Express API และคุณทำอย่างไรถึงผ่านมันมาได้?**

*คำตอบของคุณ:*
ส่วนที่ยากที่สุดคือ **การไล่หาสาเหตุของ Bug (Debugging) ระหว่างสองฝั่งและการจัดการความเข้ากันได้ของ Data Types**

เช่น ในตอนแรกเมื่อกดส่งฟอร์มจากหน้าเว็บ React ข้อมูลกลับไม่ยอมบันทึกเข้าเซิร์ฟเวอร์ สาเหตุเกิดจากข้อมูลที่รับมาจากช่อง input ใน HTML จะมีประเภทเป็น String เสมอ แต่ฝั่ง Backend มีเงื่อนไขการตรวจสอบว่าตัวเลขต้องเป็น Number และถ้าเป็นค่า 0 ตัว validation ของ Backend จะมองเป็นค่าว่างทันที ทำให้ตีกลับเป็น Error 400

ผมผ่านมันมาได้โดยการเปิดเครื่องมือ **Developer Tools (F12) ใน Browser** โดยเฉพาะแท็บ **Console** เพื่อดู error message สีแดง และแท็บ **Network** เพื่อตรวจดู Request Payload ที่ส่งออกไป รวมถึง Response ที่เซิร์ฟเวอร์ตอบกลับมาจริง ๆ การดูข้อมูลตรงนี้ช่วยให้เราไม่เดาสุ่มและสามารถปรับประเภทข้อมูลด้วย `Number()` พร้อมทั้งปรับแก้ Validation ให้รองรับได้อย่างแม่นยำ

---

## AI Process

ตอบส่วนนี้เฉพาะถ้าคุณ rate ตัวเอง **2 ขึ้นไป** บน AI Code Contribution Scale ด้านบน ถ้า rate ไว้ 0 หรือ 1 ให้เขียน "N/A" ใต้แต่ละคำถาม

**17. ถ้าคุณใช้ AI สร้างโค้ด คุณแบ่งงานออกเป็นขั้นตอนหรือ prompt อย่างไร? ยกตัวอย่าง prompt จริงที่คุณใช้ 1 อัน แทนที่จะเป็น prompt เดียวแบบ "สร้างทั้งแอปให้หน่อย"**

*คำตอบของคุณ:*
ผมใช้วิธีแบ่งงานออกเป็นขั้นตอนย่อย ๆ ทีละ Feature (Full Slice) โดยเริ่มจากการตรวจสอบและทำให้ Backend ทำงานได้ก่อน จากนั้นจึงค่อยทำ Frontend ทีละส่วน แทนที่จะสั่ง AI ทีเดียวให้สร้างทั้งหมด

**ตัวอย่าง prompt จริงที่ผมใช้:**
- *"ช่วยหา debug ในโปรเจคให้หน่อย ที่ run ไม่ได้"* (เริ่มจากการหาสาเหตุที่ทำให้เซิร์ฟเวอร์สตาร์ตไม่ติดก่อน)
- *"ต้องการ เพิ่มฟังก์ชันและปุ่ม Edit/Update สินค้า (PUT หรือ PATCH):"* (เจาะจงเฉพาะฟังก์ชันการอัปเดตข้อมูลทีละฟังก์ชัน)

---

**18. อธิบายสิ่งที่ AI tool สร้างให้ 1 อย่างที่คุณเปลี่ยน แก้ไข หรือปฏิเสธ — พร้อมเหตุผลว่าทำไม**

*คำตอบของคุณ:*
ในตอนแรก โค้ดที่ได้มามีเงื่อนไขการแสดงผลตารางที่ผูกไว้กับชื่อหัวข้อ `header` โดยเขียนไว้ว่า:
`{!header.toLowerCase().includes("react") && ...}`
ทำให้เมื่อเปิดหน้าเว็บขึ้นมาครั้งแรก ตารางสินค้าจะไม่แสดงให้เห็นเลย ผู้ใช้ต้องกดปุ่ม User หรือ Admin ก่อนตารางถึงจะยอมขึ้นมา

**สิ่งที่ผมแก้ไข:** ผมได้ปฏิเสธเงื่อนไขดังกล่าว และปรับให้ตารางแสดงผลทันทีตั้งแต่เปิดหน้าเว็บ พร้อมทั้งตั้งค่าเริ่มต้นให้เป็น `Home - User Section` เพราะตามหลัก UX ที่ดีของเว็บแคตตาล็อกสินค้า ผู้ใช้งานควรจะเห็นรายการสินค้าและค้นหาได้ทันทีโดยไม่ต้องคลิกปุ่มใด ๆ ก่อน

---

**19. อธิบาย bug หรือ error จริง ๆ ที่คุณเจอระหว่าง build โปรเจกต์นี้ 1 อย่าง คุณหาสาเหตุที่แท้จริงได้อย่างไร นอกเหนือจากการ copy error ไปถามใน chat?**

*คำตอบของคุณ:*
**Bug ที่เจอจริง:** เซิร์ฟเวอร์ Backend สตาร์ตไม่ขึ้นและฟ้อง Error:
`Error [ERR_MODULE_NOT_FOUND]: Cannot find module '../models/Product.js'`

**วิธีหาสาเหตุที่แท้จริงโดยไม่พึ่งพาแค่การ copy ข้อความ:**
ผมเปิดดูโครงสร้างของโฟลเดอร์ไฟล์ในโปรเจกต์จริง ๆ ผ่าน File Explorer / VS Code แล้วนำมาเทียบกับบรรทัดที่ 2 ของไฟล์ `server/src/routes/products.js` ที่เขียนว่า `import { products } from "../models/Product.js";` 
ทำให้เห็นความจริงว่า โฟลเดอร์ในเครื่องถูกตั้งชื่อว่า `module` (เอกพจน์) ไม่ใช่ `models` ตัว Node.js จึงไม่สามารถหาโมดูลพบได้ การตรวจเช็คโครงสร้างไฟล์ด้วยตัวเองทำให้เข้าใจเรื่อง Relative Path และแก้ไขชื่อพาธได้สำเร็จอย่างแท้จริง

นอกจากนี้ยังมีจุดพิมพ์ตกในฟอร์มของไฟล์ `Home.jsx` บรรทัดที่ใส่ attribute ว่า `ame="price"` ขาดตัวอักษร `n` ไป ทำให้เวลากรอกราคา ค่าไม่ถูกเซฟลง State ซึ่งการไล่ดูโค้ดทีละบรรทัดช่วยให้ตรวจจับข้อผิดพลาดเล็ก ๆ เหล่านี้ได้

---

**20. เลือก route (backend) หรือ component (frontend) 1 อันที่ AI ช่วยสร้าง โดยไม่ย้อนกลับไปดู AI chat history อธิบายว่ามันทำอะไรและทำไมถึงทำงาน ด้วยคำพูดของคุณเอง**

*คำตอบของคุณ (เลือก Route `GET /products` ที่ทำงานร่วมกับ Search Bar):*
- **สิ่งที่มันทำ:** เป็น Route ที่ทำหน้าที่ดึงข้อมูลสินค้าทั้งหมด แต่มีความพิเศษคือสามารถรองรับการกรองข้อมูลตามชื่อสินค้าได้ผ่าน Query string
- **ทำไมถึงทำงานได้ (อธิบายการทำงาน):**
  1. เมื่อผู้ใช้พิมพ์ข้อความลงในช่อง Search Bar บนหน้าเว็บ React ตัว `useEffect` ที่มีฟังก์ชัน Debounce จะรอให้ผู้ใช้พิมพ์เสร็จครู่หนึ่ง แล้วยิงคำขอไปที่ URL เช่น `GET /products?name=walk`
  2. เมื่อคำขอมาถึงฝั่ง Express ตัว Router จะหยิบค่าคำค้นหามาจากตัวแปร `req.query.name`
  3. เซิร์ฟเวอร์จะนำ array ของสินค้าทั้งหมดมาผ่านฟังก์ชัน `.filter()` และใช้คำสั่ง `.includes()` ร่วมกับ `.toLowerCase()` เพื่อเปรียบเทียบชื่อสินค้าว่ามีคำค้นหานั้นอยู่หรือไม่ โดยไม่สนใจว่าจะเป็นตัวพิมพ์เล็กหรือตัวพิมพ์ใหญ่
  4. เมื่อกรองเสร็จ เซิร์ฟเวอร์จะส่ง array ผลลัพธ์กลับไปด้วย Status `200 OK` และฝั่ง React จะนำข้อมูลชุดนี้ไปอัปเดตลงใน State เพื่อแสดงผลเฉพาะอัลบั้มที่ตรงกับคำค้นหาทันที
